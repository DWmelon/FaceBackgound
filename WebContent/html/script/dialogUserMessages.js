/*$.get("login",function(data){
	var jsonData = $.parseJSON(data);
	$(".customer-img").attr("src",data.icon_url);
	$(".customer-name label").html(data.name);
	$(".type-name label"),html(data.accountType);
	var id = data.id;
	var test = data.isture;
	if(test == '1'){
		$(".type-verify").html("已认证");
	}else{
		$(".type-verify").html("未认证");
	}

	$("#user-id").html("id");
})
*/